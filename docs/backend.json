
{
  "entities": {
    "user": {
      "title": "User",
      "description": "A user of the application.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The user's name."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "profile": {
          "type": "string",
          "description": "The user's access profile name."
        }
      },
      "required": [
        "name",
        "email",
        "profile"
      ]
    },
    "accessProfile": {
      "title": "Access Profile",
      "description": "Defines a user's access profile and permissions.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the access profile."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the profile's permissions."
        }
      },
      "required": [
        "name"
      ]
    },
    "accountPlan": {
      "title": "Plano de Contas",
      "description": "Uma conta no plano de contas para categorizar despesas.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "O nome da conta."
        },
        "description": {
          "type": "string",
          "description": "Uma breve descrição da conta."
        },
        "parentId": {
          "type": ["string", "null"],
          "description": "O ID da conta pai. Nulo para contas de nível raiz."
        }
      },
      "required": [
        "name"
      ]
    },
    "resultCenter": {
      "title": "Result Center",
      "description": "A result center for allocating expenses.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the result center."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the result center."
        }
      },
      "required": [
        "name"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/user"
      },
      "description": "User profile information."
    },
    "/accessProfiles/{profileId}": {
      "schema": {
        "$ref": "#/entities/accessProfile"
      },
      "description": "Access profile definitions."
    },
    "/accountPlans/{planId}": {
      "schema": {
        "$ref": "#/entities/accountPlan"
      },
      "description": "Definições do plano de contas."
    },
    "/resultCenters/{centerId}": {
      "schema": {
        "$ref": "#/entities/resultCenter"
      },
      "description": "Result center definitions."
    }
  }
}
